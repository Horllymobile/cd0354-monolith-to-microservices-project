apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-feed-deployment
  labels:
    app: udagram-api-feed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: udagram-api-feed
  template:
    metadata:
      labels:
        app: udagram-api-feed
    spec:
      containers:
        - image: horllymobile/udagram-api-feed:v1
          name: udagram-api-feed
          imagePullPolicy: Always          
          resources:
            requests:
              memory: "2048Mi"
              cpu: "250m"
            limits:
              memory: "4096Mi"
              cpu: "500m"       
          ports:
          - containerPort: 8080
      restartPolicy: Always